[tool.black]
line-length = 88
target-version = ['py310']
include = '\.pyi?$'

[tool.ruff]
line-length = 88
target-version = 'py310'
extend-select = ['I']
exclude = ['.git', '__pycache__', 'env', 'venv', 'build', 'dist']

[tool.isort]
profile = 'black'
